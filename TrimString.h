#pragma once

int checkID(char id[]);
char *rightName(char *name);
char* strupr(char* s);
char* strlwr(char* s);

char* lTrim(char s[]);
char* rTrim(char s[]);
char* trim(char s[]);
char* nameStr(char s[]); 